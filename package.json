{
  "name": "rxt",
  "module": "app.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build-proj": " --env-file=.env.production bun run build app.ts --production --out-dir ./build --format esm --minify --sourcemap=inline",
    "dev": "bun --hot app.ts"
  }
}