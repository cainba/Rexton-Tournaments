{
  "name": "rxt",
  "module": "app.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "pack": "bun build app.ts --minify --experimentlal-css --experimental-html --splitting --target=bun --outdir=./"
  }
}