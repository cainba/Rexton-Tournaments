{
  "name": "rxt",
  "module": "app.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build-proj": "bun build app.ts --production --outdir ./build --format esm --minify --sourcemap=inline --publicPath='https://rextongaming.com' && cp -r ./index.html ./rxt.css ./build ",
    "dev": "bun --hot app.ts"
  }
}