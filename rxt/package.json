{
  "name": "@rxt/app",
  "private": "true",
  "module": "rxt.ts",
  "type": "module",
  "workspaces": [
    "packages/server",
    "packages/db",
    "packages/client",
    "packages/public"
  ],
  "devDependencies": {
    "bun": "^1.1.4",
    "typescript": "latest"
  },
  "scripts": {
    "clean": "rm -f -v -R dist && rm -f -v -R node_modules"
  }
}
